'use client'

import { useEffect, useRef, useState } from 'react'
import { ArrowLeft, Download, Eraser, Minus, Plus, RotateCcw, RotateCw, Sparkles } from 'lucide-react'

const drawings = [
  { id: 'castle', name: 'Princess Castle', emoji: '♕', src: '/coloring/castle.png', art: `<path d="M102 280V150h48v-62h42v62h16V88h42v62h48v130Z"/><path d="M84 280V125h38v43H84M316 280V125h-38v43h38M102 150h48M250 150h48M171 280v-68q29-42 58 0v68M119 198h31M250 198h31M111 125l20-32 19 32M269 125l-19-32 20 32M190 88l10-35 10 35"/><path d="M142 232q-22-35 9-54 27-16 46 12 19-28 46-12 31 19 9 54-27 34-55 46-28-12-55-46Z"/><circle cx="154" cy="201" r="5"/><circle cx="246" cy="201" r="5"/><path d="M170 225q30 20 60 0"/>` },
  { id: 'royal-cats', name: 'Royal Kittens', emoji: '♡', src: '/coloring/kittens.png', art: `<path d="M78 270q-12-92 47-120l-10-77 55 40q30-16 60 0l55-40-10 77q59 28 47 120Z"/><path d="M112 180q-18-36 13-56l28 24q-12 31-41 32ZM288 180q18-36-13-56l-28 24q12 31 41 32Z"/><circle cx="151" cy="185" r="7"/><circle cx="249" cy="185" r="7"/><path d="M177 215q23 22 46 0M200 205v15M108 235H59M110 251H66M292 235h49M290 251h44M144 139l-18-31M256 139l18-31M130 260q22-24 42 0M270 260q-22-24-42 0"/><path d="m200 247 5 11 12 2-9 8 3 12-11-7-11 7 3-12-9-8 12-2Z"/>` },
  { id: 'teddy-date', name: 'Teddy Bear Date', emoji: '♡', src: '/coloring/bears.png', art: `<circle cx="140" cy="135" r="42"/><circle cx="260" cy="135" r="42"/><circle cx="140" cy="190" r="75"/><circle cx="260" cy="190" r="75"/><circle cx="140" cy="135" r="22"/><circle cx="260" cy="135" r="22"/><circle cx="125" cy="183" r="7"/><circle cx="155" cy="183" r="7"/><circle cx="245" cy="183" r="7"/><circle cx="275" cy="183" r="7"/><path d="M128 214q12 14 24 0M248 214q12 14 24 0M140 205v15M260 205v15M110 250v35M170 250v35M230 250v35M290 250v35M200 175q-15 20 0 40 15-20 0-40Z"/><path d="m200 76 6 13 14 2-10 10 3 14-13-7-13 7 3-14-10-10 14-2Z"/>` },
  { id: 'love-birds', name: 'Love Birds', emoji: '♥', src: '/coloring/swans.png', art: `<path d="M200 270V130M200 170q-55-80-105-22-35 42 53 86M200 170q55-80 105-22 35 42-53 86"/><circle cx="120" cy="111" r="42"/><circle cx="280" cy="111" r="42"/><path d="M105 111h-25l22 15M295 111h25l-22 15M120 153q34 38 80 39M280 153q-34 38-80 39M156 270q-28 24-57 0M244 270q28 24 57 0"/><circle cx="130" cy="105" r="7"/><circle cx="270" cy="105" r="7"/><path d="m200 198 7 14 15 2-11 10 3 15-14-8-14 8 3-15-11-10 15-2Z"/>` },
  { id: 'rose-garden', name: 'Rose Garden', emoji: '✿', src: '/coloring/princess.png', art: `<path d="M200 286V130M200 220q-55-30-93 6M200 245q55-30 93 6M200 175q-30-34-60-18M200 190q30-34 60-18M107 286H293"/><path d="M200 130q-42-24-37-62 3-27 37-39 34 12 37 39 5 38-37 62Z"/><path d="M163 83q37-20 74 0M166 111q34-22 68 0M107 226q-31-39-2-65 29 26 2 65ZM293 226q31-39 2-65-29 26-2 65Z"/><path d="m200 29 5 10 11 2-8 8 2 11-10-6-10 6 2-11-8-8 11-2Z"/>` },
  { id: 'moon-couple', name: 'Moonlight Date', emoji: '☾', src: '/coloring/bunnies.png', art: `<path d="M108 272q-8-102 51-125 41-16 65 22 24-38 65-22 59 23 51 125Z"/><path d="M200 170q-25-75 35-113 33 51 3 91M200 170q25-75-35-113-33 51-3 91"/><circle cx="161" cy="197" r="10"/><circle cx="239" cy="197" r="10"/><path d="M175 230q25 25 50 0M200 215v18M128 260q25-20 48 0M224 260q23-20 48 0"/><path d="m200 79 5 11 12 2-9 8 3 13-11-7-11 7 3-13-9-8 12-2ZM90 90l4 8 9 1-7 6 2 10-8-5-8 5 2-10-7-6 9-1ZM310 90l4 8 9 1-7 6 2 10-8-5-8 5 2-10-7-6 9-1Z"/>` },
  { id: 'love-lane', name: 'Love Lane', emoji: '♥', src: '/coloring/unicorn.png', art: `<path d="M85 285q18-118 115-158 97 40 115 158Z"/><path d="M200 127Q150 73 105 107q-26 20 18 61 43-40 77-1 34-39 77 1 44-41 18-61-45-34-95 20Z"/><path d="M140 285q12-84 60-126 48 42 60 126M120 238h160M104 276h192"/><path d="M82 202q30 18 48-8M318 202q-30 18-48-8M88 165l-15-32M312 165l15-32"/><path d="m200 157 6 12 13 2-10 9 3 13-12-7-12 7 3-13-10-9 13-2Z"/>` },
  { id: 'corgi-couple', name: 'Corgi Sweethearts', emoji: '●', src: '/coloring/picnic.png', art: `<path d="M83 155 78 79l54 34q25-17 50 0l54-34-5 76q20 27 12 78-11 62-80 62t-80-62q-8-51 12-78ZM317 155l5-76-54 34q-25-17-50 0l-54-34 5 76q-20 27-12 78 11 62 80 62t80-62q8-51-12-78Z"/><circle cx="145" cy="184" r="8"/><circle cx="255" cy="184" r="8"/><path d="M158 214q17 15 34 0M242 214q-17 15-34 0M192 202q8 8 16 0M208 202q-8 8-16 0M104 246l-34 12M296 246l34 12"/><path d="m200 232 6 12 13 2-10 9 3 13-12-7-12 7 3-13-10-9 13-2Z"/>` },
  { id: 'heart-cottage', name: 'Heart Cottage', emoji: '⌂', src: '/coloring/cottage.png', art: `<path d="M90 280V165l110-93 110 93v115Z"/><path d="m76 169 124-112 124 112M154 280v-70q46-45 92 0v70M112 191h39v42h-39ZM249 191h39v42h-39ZM190 121h20v28h-20Z"/><path d="M120 280q20-42 40 0M240 280q20-42 40 0M200 70V42M185 55h30"/><path d="m200 171 6 12 13 2-10 9 3 13-12-7-12 7 3-13-10-9 13-2Z"/>` },
  { id: 'swan-love', name: 'Swan Lake', emoji: '♡', src: '/coloring/rainbow-unicorn.png', art: `<path d="M70 270q55-55 108-38 22 7 43-2 34-15 87 40-85 33-170 0-34 17-68 0Z"/><path d="M120 243q-32-55 11-103 26-29 42-72 17 62-3 91-18 26-7 84M280 243q32-55-11-103-26-29-42-72-17 62 3 91 18 26 7 84"/><path d="M142 126q-25-20-48-5l25 13M258 126q25-20 48-5l-25 13M151 177q22 18 44 0M249 177q-22 18-44 0M145 231q27-20 55 0M255 231q-27-20-55 0"/><path d="m200 99 6 12 13 2-10 9 3 13-12-7-12 7 3-13-10-9 13-2Z"/>` },
]

const colors = ['#ef8f9d', '#f6bd60', '#f7df75', '#9bcf9b', '#83c5be', '#8db8e8', '#b7a0d8', '#d59bb7', '#f4a261', '#5c6470']
const svgFor = (art: string) => `data:image/svg+xml;charset=utf-8,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 330"><rect width="400" height="330" fill="white"/><g fill="white" stroke="#24252b" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"><path d="M28 278q18-22 36 0t36 0 36 0 36 0 36 0 36 0 36 0 36 0 36 0 36 0"/><path d="M42 84q15-22 30 0 18-16 35 2-12 17-35 10-22 7-30-12ZM292 68q14-19 28 0 17-14 31 3-10 15-31 9-20 6-28-12Z"/><path d="m52 177 5 11 12 2-9 8 3 13-11-7-11 7 3-13-9-8 12-2ZM348 191l4 9 10 2-8 7 2 10-8-6-9 6 3-10-8-7 10-2Z"/><path d="M48 278v-38M48 252l-17-18M48 258l18-19M352 278v-38M352 252l-17-18M352 258l18-19"/>${art}</g></svg>` )}`

function Gallery({ onSelect }: { onSelect: (id: string) => void }) {
  return <main className="gallery-shell"><header className="gallery-header"><div className="brand-mark"><Sparkles size={20} strokeWidth={2.5} /></div><div><p className="eyebrow">For two creative hearts</p><h1>My Coloring Book</h1><p className="subtitle">Choose a sweet scene and color it together.</p></div></header><section className="gallery-content"><div className="section-heading"><div><p className="eyebrow">The collection</p><h2>Little scenes for two</h2></div><span className="count">{drawings.length} drawings</span></div><div className="drawing-grid">{drawings.map((drawing, index) => <button className="drawing-card" key={drawing.id} onClick={() => onSelect(drawing.id)}><div className="art-thumb"><img src={drawing.src} alt={`${drawing.name} detailed coloring page`} /><span className="art-badge">{drawing.emoji}</span></div><div className="card-label"><span>{drawing.name}</span><span className="card-arrow">→</span></div><span className="sr-only">Open {drawing.name}</span></button>)}</div></section><footer className="gallery-footer"><span>Made for quiet, creative moments</span><span className="footer-dot" /> <span>Save your colors as you go</span></footer></main>
}

type FillResult = { changed: boolean; mask: Uint8Array; bounds: { left: number; top: number; right: number; bottom: number } }

function floodFill(ctx: CanvasRenderingContext2D, x: number, y: number, color: string): FillResult {
  const { width, height } = ctx.canvas
  const image = ctx.getImageData(0, 0, width, height)
  const pixels = image.data
  const px0 = Math.max(0, Math.min(width - 1, Math.floor(x)))
  const py0 = Math.max(0, Math.min(height - 1, Math.floor(y)))
  const start = (py0 * width + px0) * 4
  const target = [pixels[start], pixels[start + 1], pixels[start + 2], pixels[start + 3]]
  const fill = hexToRgb(color)
  const mask = new Uint8Array(width * height)
  const bounds = { left: px0, top: py0, right: px0, bottom: py0 }
  const empty = { changed: false, mask, bounds }
  if (target[3] < 10 || Math.max(target[0], target[1], target[2]) < 90) return empty
  if (target.every((value, index) => index === 3 || Math.abs(value - fill[index]) < 3)) return empty
  const queue: number[] = [py0 * width + px0]
  const seen = new Uint8Array(width * height)
  let filledPixels = 0
  const maxFillPixels = Math.floor(width * height * .72)
  const isOpen = (pos: number) => { const i = pos * 4; const brightness = (pixels[i] + pixels[i + 1] + pixels[i + 2]) / 3; const edge = Math.min(pixels[i], pixels[i + 1], pixels[i + 2]); return pixels[i + 3] > 10 && brightness > 105 && edge > 62 && Math.max(Math.abs(pixels[i] - target[0]), Math.abs(pixels[i + 1] - target[1]), Math.abs(pixels[i + 2] - target[2])) < 38 }
  while (queue.length && filledPixels < maxFillPixels) { const pos = queue.pop()!; if (pos < 0 || pos >= width * height || seen[pos] || !isOpen(pos)) continue; seen[pos] = 1; mask[pos] = 1; filledPixels += 1; const px = pos % width; const py = Math.floor(pos / width); bounds.left = Math.min(bounds.left, px); bounds.right = Math.max(bounds.right, px); bounds.top = Math.min(bounds.top, py); bounds.bottom = Math.max(bounds.bottom, py); queue.push(pos - 1, pos + 1, pos - width, pos + width) }
  if (!mask[(py0 * width) + px0]) return empty
  for (let pos = 0; pos < mask.length; pos++) if (mask[pos]) { const i = pos * 4; pixels[i] = fill[0]; pixels[i + 1] = fill[1]; pixels[i + 2] = fill[2]; pixels[i + 3] = 255 }
  ctx.putImageData(image, 0, 0)
  return { changed: true, mask, bounds }
}
function hexToRgb(hex: string) { const n = Number.parseInt(hex.slice(1), 16); return [(n >> 16) & 255, (n >> 8) & 255, n & 255] }

function Editor({ drawing, onBack }: { drawing: typeof drawings[number]; onBack: () => void }) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const viewportRef = useRef<HTMLDivElement>(null)
  const [color, setColor] = useState(colors[0])
  const [history, setHistory] = useState<ImageData[]>([])
  const [future, setFuture] = useState<ImageData[]>([])
  const [zoom, setZoom] = useState(100)
  const [pan, setPan] = useState({ x: 0, y: 0 })
  const dragRef = useRef({ active: false, moved: false, startX: 0, startY: 0, panX: 0, panY: 0 })
  const pointersRef = useRef(new Map<number, { x: number; y: number }>())
  const pinchRef = useRef<{ distance: number; zoom: number } | null>(null)
  const initialRef = useRef<ImageData | null>(null)
  const [hoverPoint, setHoverPoint] = useState<{ x: number; y: number } | null>(null)
  const [ripple, setRipple] = useState<{ x: number; y: number; color: string } | null>(null)
  const [status, setStatus] = useState('')
  const suppressClickRef = useRef(false)
  const zoomLevels = [50, 75, 100, 125, 150, 200, 250, 300]
  useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; canvas.width = 800; canvas.height = 660; const ctx = canvas.getContext('2d')!; const img = new Image(); img.onload = () => { ctx.drawImage(img, 0, 0, 800, 660); const saved = localStorage.getItem(`coloring-${drawing.id}`); if (saved) { const restored = new Image(); restored.onload = () => { ctx.drawImage(restored, 0, 0); initialRef.current = ctx.getImageData(0, 0, 800, 660) }; restored.src = saved } else initialRef.current = ctx.getImageData(0, 0, 800, 660) }; const savedZoom = Number(localStorage.getItem(`coloring-${drawing.id}-zoom`)); if (zoomLevels.includes(savedZoom)) setZoom(savedZoom); setPan({ x: 0, y: 0 }); setHistory([]); setFuture([]); img.src = drawing.src }, [drawing])
  useEffect(() => { localStorage.setItem(`coloring-${drawing.id}-zoom`, String(zoom)); if (zoom <= 100) setPan({ x: 0, y: 0 }) }, [drawing.id, zoom])
  const snapshot = () => canvasRef.current?.getContext('2d')?.getImageData(0, 0, 800, 660)
  const save = () => { const canvas = canvasRef.current; if (canvas) localStorage.setItem(`coloring-${drawing.id}`, canvas.toDataURL('image/png')) }
  const setZoomLevel = (next: number) => { setZoom(Math.max(50, Math.min(300, next))); setPan({ x: 0, y: 0 }) }
  const stepZoom = (direction: 1 | -1) => { const index = zoomLevels.indexOf(zoom); setZoomLevel(zoomLevels[Math.max(0, Math.min(zoomLevels.length - 1, index + direction))]) }
  const getCanvasPoint = (event: { clientX: number; clientY: number }) => { const canvas = canvasRef.current; if (!canvas) return null; const rect = canvas.getBoundingClientRect(); return { x: ((event.clientX - rect.left) / rect.width) * canvas.width, y: ((event.clientY - rect.top) / rect.height) * canvas.height } }
  const paint = (event: React.MouseEvent<HTMLCanvasElement>) => { if (suppressClickRef.current || pointersRef.current.size > 1) { suppressClickRef.current = false; return } const point = getCanvasPoint(event); const canvas = canvasRef.current; const ctx = canvas?.getContext('2d'); if (!point || !canvas || !ctx) return; const before = snapshot(); const result = floodFill(ctx, point.x, point.y, color); if (!result.changed) return; if (before) setHistory(prev => [...prev.slice(-19), before]); setFuture([]); setRipple({ x: point.x, y: point.y, color }); setStatus('Saved'); window.setTimeout(() => setRipple(null), 480); window.setTimeout(() => setStatus(''), 1400); save() }
  const pointerDown = (event: React.PointerEvent<HTMLCanvasElement>) => { pointersRef.current.set(event.pointerId, { x: event.clientX, y: event.clientY }); event.currentTarget.setPointerCapture(event.pointerId); if (pointersRef.current.size === 2) { const points = [...pointersRef.current.values()]; pinchRef.current = { distance: Math.hypot(points[0].x - points[1].x, points[0].y - points[1].y), zoom }; return } dragRef.current = { active: zoom > 100, moved: false, startX: event.clientX, startY: event.clientY, panX: pan.x, panY: pan.y } }
  const pointerMove = (event: React.PointerEvent<HTMLCanvasElement>) => { const point = { x: event.clientX, y: event.clientY }; pointersRef.current.set(event.pointerId, point); if (pointersRef.current.size > 1 && pinchRef.current) { const points = [...pointersRef.current.values()]; const distance = Math.hypot(points[0].x - points[1].x, points[0].y - points[1].y); setZoomLevel(zoomLevels.reduce((best, level) => Math.abs(level - pinchRef.current!.zoom * distance / pinchRef.current!.distance) < Math.abs(best - pinchRef.current!.zoom * distance / pinchRef.current!.distance) ? level : best, 100)); return } if (!dragRef.current.active) { setHoverPoint(getCanvasPoint(event)); return } const dx = event.clientX - dragRef.current.startX; const dy = event.clientY - dragRef.current.startY; if (Math.hypot(dx, dy) > 3) dragRef.current.moved = true; if (dragRef.current.moved) { suppressClickRef.current = true; setPan({ x: dragRef.current.panX + dx, y: dragRef.current.panY + dy }) } }
  const pointerUp = (event: React.PointerEvent<HTMLCanvasElement>) => { pointersRef.current.delete(event.pointerId); if (pointersRef.current.size < 2) pinchRef.current = null; if (dragRef.current.moved) suppressClickRef.current = true; dragRef.current.active = false }
  const handleWheel = (event: React.WheelEvent<HTMLDivElement>) => { if (!event.ctrlKey) return; event.preventDefault(); stepZoom(event.deltaY < 0 ? 1 : -1) }
  const undo = () => { const ctx = canvasRef.current?.getContext('2d'); const item = history.at(-1); const current = snapshot(); if (!ctx || !item || !current) return; ctx.putImageData(item, 0, 0); setHistory(history.slice(0, -1)); setFuture([current, ...future]); save() }
  const redo = () => { const ctx = canvasRef.current?.getContext('2d'); const item = future[0]; const current = snapshot(); if (!ctx || !item || !current) return; ctx.putImageData(item, 0, 0); setFuture(future.slice(1)); setHistory([...history, current]); save() }
  const clear = () => { if (!window.confirm('Clear all your colors from this drawing?')) return; const ctx = canvasRef.current?.getContext('2d'); const current = snapshot(); if (!ctx || !current || !initialRef.current) return; setHistory(prev => [...prev, current]); setFuture([]); ctx.putImageData(initialRef.current, 0, 0); setStatus('Cleared'); window.setTimeout(() => setStatus(''), 1200); save() }
  const download = () => { const link = document.createElement('a'); link.download = `${drawing.name.toLowerCase()}-my-coloring-book.png`; link.href = canvasRef.current?.toDataURL('image/png') || ''; link.click() }
  return <main className="editor-shell"><header className="editor-top"><button className="back-button" onClick={onBack}><ArrowLeft size={18} /> Back</button><div className="editor-title"><span className="eyebrow">Now coloring</span><h1>{drawing.name}</h1></div><button className="download-button" onClick={download}><Download size={17} /> <span>Download</span></button></header><section className="canvas-stage"><div className={`canvas-frame ${zoom > 100 ? 'is-zoomed' : ''}`} ref={viewportRef} onWheel={handleWheel}><div className="canvas-viewport" style={{ transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom / 100})` }}><canvas ref={canvasRef} onPointerDown={pointerDown} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerCancel={pointerUp} onPointerLeave={() => setHoverPoint(null)} onClick={paint} aria-label={`Coloring canvas for ${drawing.name}`} />{hoverPoint && <span className="hover-preview" style={{ left: `${(hoverPoint.x / 800) * 100}%`, top: `${(hoverPoint.y / 660) * 100}%`, backgroundColor: color }} aria-hidden="true" />}{ripple && <span className="fill-ripple" style={{ left: `${(ripple.x / 800) * 100}%`, top: `${(ripple.y / 660) * 100}%`, backgroundColor: ripple.color }} aria-hidden="true" />}</div></div><p className="canvas-tip">Pick a color, then tap a space to fill it in · Ctrl + scroll to zoom · Drag to move</p>{status && <span className="save-status" role="status">{status}</span>}</section><section className="toolbar" aria-label="Coloring tools"><div className="tool-group"><button className="tool-button" onClick={undo} disabled={!history.length} aria-label="Undo" title="Undo"><RotateCcw size={18} strokeWidth={2.2} /><span>Undo</span></button><button className="tool-button" onClick={redo} disabled={!future.length} aria-label="Redo" title="Redo"><RotateCw size={18} strokeWidth={2.2} /><span>Redo</span></button><button className="tool-button" onClick={clear} aria-label="Clear drawing" title="Clear drawing"><Eraser size={18} strokeWidth={2.2} /><span>Clear</span></button></div><div className="toolbar-divider" aria-hidden="true" /><div className="zoom-controls" aria-label="Zoom controls"><button className="tool-button" onClick={() => stepZoom(-1)} disabled={zoom === 50} aria-label="Zoom out" title="Zoom out"><Minus size={17} /></button><span className="zoom-value" aria-live="polite">{zoom}%</span><button className="tool-button" onClick={() => stepZoom(1)} disabled={zoom === 300} aria-label="Zoom in" title="Zoom in"><Plus size={17} /></button><button className="fit-button" onClick={() => setZoomLevel(100)} aria-label="Fit canvas" title="Fit canvas">Fit</button></div><div className="toolbar-divider" aria-hidden="true" /><div className="palette"><span className="palette-label">Colors</span><div className="swatch-list">{colors.map((item) => <button key={item} className={`swatch ${color === item ? 'selected' : ''}`} style={{ backgroundColor: item }} onClick={() => setColor(item)} aria-label={`Choose ${item}`} title={`Choose ${item}`} />)}<label className="custom-color" aria-label="Choose custom color" title="Choose custom color"><Plus size={17} strokeWidth={2.5} /><input type="color" value={color} onChange={(e) => setColor(e.target.value)} /></label></div></div></section></main>
}

export default function Page() { const [selected, setSelected] = useState<string | null>(null); const drawing = drawings.find((item) => item.id === selected); return drawing ? <Editor drawing={drawing} onBack={() => setSelected(null)} /> : <Gallery onSelect={setSelected} /> }
